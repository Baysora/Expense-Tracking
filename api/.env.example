# Prisma reads from this file (not local.settings.json)
# Copy to .env and fill in your values

# Local development (Docker SQL Server)
DATABASE_URL="sqlserver://localhost:1433;database=ExpenseTracking;user=sa;password=YourPass123!;encrypt=false;trustServerCertificate=true"

# Azure SQL (production)
# DATABASE_URL="sqlserver://<server>.database.windows.net;database=ExpenseTracking;user=<user>;password=<pass>;encrypt=true"
