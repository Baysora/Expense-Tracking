name: Deploy to Azure Static Web Apps

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  build_and_deploy:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          lfs: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Build shared first and produce a tarball so the API can install it
      # without relying on workspace symlinks (which break in Oryx/zip contexts)
      - name: Build and pack shared package
        working-directory: packages/shared
        run: |
          npm install
          npm run build
          npm pack --pack-destination ../../api/

      # Install workspace deps — npm ci pins exact versions from the lock file
      - name: Install workspace dependencies
        run: npm ci

      # Compile the API TypeScript using workspace-resolved types
      # api/dist/ is uploaded to SWA along with the source; Oryx won't re-compile
      - name: Build API
        working-directory: api
        run: |
          npx prisma generate
          npx tsc

      - name: Build frontend
        run: npm run build --workspace=frontend
        env:
          VITE_ENTRA_TENANT_NAME: ${{ secrets.ENTRA_TENANT_NAME }}
          VITE_ENTRA_CLIENT_ID: ${{ secrets.ENTRA_CLIENT_ID }}
          VITE_API_SCOPE: ${{ secrets.ENTRA_API_SCOPE }}
          VITE_DEV_MODE: 'false'

      - name: Deploy to Azure Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: 'upload'
          app_location: 'frontend/dist'
          api_location: 'api'
          output_location: ''
          skip_app_build: true
          # skip_api_build is intentionally omitted — Oryx must run so SWA can
          # detect the Node.js runtime and configure the Function host.
          # Oryx runs: npm install (picks up expense-shared tarball) + api_build_command.
          # Our pre-compiled api/dist/ travels with the upload unchanged.
          api_build_command: 'npx prisma generate'
          config_file_location: '.'
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          AZURE_STORAGE_CONNECTION_STRING: ${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}
          AZURE_STORAGE_CONTAINER_NAME: expense-attachments
          ENTRA_TENANT_NAME: ${{ secrets.ENTRA_TENANT_NAME }}
          ENTRA_AUDIENCE: ${{ secrets.ENTRA_AUDIENCE }}

  close_pull_request:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request
    steps:
      - name: Close Pull Request
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          action: 'close'
